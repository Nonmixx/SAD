
  # Student Housing Finder App

  This is a code bundle for Student Housing Finder App. The original project is available at https://www.figma.com/design/BySYb5PDv1WkTMRfFmMqoz/Student-Housing-Finder-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  